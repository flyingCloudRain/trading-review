# Streamlit Cloud Secrets 配置文件示例
# 
# 使用方法：
# 1. 复制此文件为 secrets.toml（本地开发使用）
# 2. 或在 Streamlit Cloud 的 Secrets 页面直接粘贴此内容
#
# ⚠️ 注意：
# - 不要将包含真实密码的 secrets.toml 提交到 Git
# - 此文件已添加到 .gitignore
# - Streamlit Cloud 中的 Secrets 会自动加密存储

# ============================================================================
# 方式 1: 使用 URI 连接（推荐，最简单）
# ============================================================================

# 连接池 URI（强烈推荐，避免 IPv6 问题）
# 从 Supabase Dashboard -> Settings -> Database -> Connection Pooler URL 获取
# 格式：postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_POOLER_URL = "postgresql://postgres.uvtmbjgndhcmlupridss:[你的密码]@aws-0-[区域].pooler.supabase.com:6543/postgres?pgbouncer=true"

# 或使用标准连接 URI
# 从 Supabase Dashboard -> Settings -> Database -> Connection string 获取
# DATABASE_URL = "postgresql://postgres:[你的密码]@db.uvtmbjgndhcmlupridss.supabase.co:5432/postgres"

# ============================================================================
# 方式 2: 使用分离的配置（向后兼容）
# ============================================================================

# 项目引用 ID（从 Settings -> General -> Reference ID 获取）
# SUPABASE_PROJECT_REF = "uvtmbjgndhcmlupridss"

# 数据库密码（从 Settings -> Database -> Database password 获取）
# SUPABASE_DB_PASSWORD = "你的数据库密码"

# ============================================================================
# 可选配置
# ============================================================================

# Supabase 项目 URL（从 Settings -> API -> Project URL 获取）
# SUPABASE_URL = "https://uvtmbjgndhcmlupridss.supabase.co"

# Supabase API 密钥（从 Settings -> API -> anon public key 获取）
# SUPABASE_ANON_KEY = "你的 anon key"

# ============================================================================
# 配置说明
# ============================================================================
#
# 优先级（从高到低）：
# 1. DATABASE_POOLER_URL - 连接池 URI（推荐）
# 2. DATABASE_URL - 完整连接 URI
# 3. SUPABASE_PROJECT_REF + SUPABASE_DB_PASSWORD - 分离配置
#
# 推荐配置：
# - 优先使用 DATABASE_POOLER_URL（连接池，避免 IPv6 问题）
# - 如果连接池不可用，使用 DATABASE_URL
# - 如果都不配置，使用 SUPABASE_PROJECT_REF 和 SUPABASE_DB_PASSWORD
#
# 获取配置信息：
# 1. 访问 https://supabase.com/dashboard
# 2. 选择你的项目
# 3. 进入 Settings -> Database
# 4. 复制 Connection Pooler URL 或 Connection string
#
# ============================================================================

